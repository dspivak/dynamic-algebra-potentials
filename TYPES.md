# Types module (auto-extracted draft)

This file is auto-extracted from `dynamic-algebra-potentials.tex` and lists the named mathematical structures the paper introduces. It is intended to be hand-curated and to serve as the shared vocabulary for the upcoming code implementation.

Each bullet has the form `- **\name** (introduced in \cref{label}) -- one-line description.`

## Categories

- **`\finset`** (= `\Cat{Fin}`) (introduced in \cref{ex.lens_finsetop}) -- \finset\op$, opposite to the category of finite sets, is cartesian monoidal where the product is given by the disjoint union coproduct $(0,+)$, i.e.\ the coproduct in $\smset$. A comonoid in $\smset\o
- **`\mfd`** (= `\Cat{Mfd}`) (introduced in \cref{sec.manifolds_notation}) -- \mfd,\rr^0,\times)$ of finite-dimensional smooth real manifolds and smooth maps. For a manifold $M$ and point $m\colon\rr^0\to M$, write $T_mM$ for the tangent space at $m$ and $TM$ for the tangent bu
- **`\Para`** (= `\Cat{Para}`) (introduced in \cref{subsec.dynamics_functor}) -- \Para$ constructions (\cref{sec.para_general}), which we denote \[ \para{\pvect}{\lmfd^\rr}\qqand \para{\cot}{\poly}\qqand \para{\cot{T^*}}{\poly}. \] \begin{definition}\label{def.potlens} The symmetr
- **`\pnla`** (= `\Cat{PNLA}`) (introduced in \cref{rmk.pnla_generalization}) -- \pnla$ throughout, \cref{prop.pnla_monoidal,prop.canonical_symplectic_pairing,prop.TT_monoidal,prop.legendre} hold verbatim; the rest pick up Lie-group ingredients: \begin{itemize} \item \Cref{prop.pn
- **`\poly`** (= `\Cat{Poly}`) (introduced in \cref{sec.poly}) -- \poly$}\label{sec.poly} We briefly review the category $\poly$ of polynomial functors; see~\cite{niu2025polynomial} for a comprehensive treatment. A \emph{polynomial functor} is an endofunctor $p\colo
- **`\potlens`** (= `\Cat{PLens}`) (introduced in \cref{ch.potentials}) -- \potlens$ of potentialized lenses. \Cref{sec.lenses_general} develops lenses, their wiring-diagram operations, and the backward-monad construction. \Cref{sec.abstract_representation} packages the repr
- **`\pvect`** (= `\Cat{PVect}`) (introduced in \cref{def.pnla}) -- \pvect$ to have paired vector spaces as objects, and as morphisms $(V,\sharp_V)\to(W,\sharp_W)$ the linear isomorphisms $\phi\colon V\To{\cong}W$ preserving the pairing: $\pairing{\phi v,\phi v'}_W=\p
- **`\smcat`** (= `\Cat{Cat}`) (introduced in \cref{sec.org}) -- \smcat,1,\times)$ is lax monoidal: \[ 1\to\yon\coalg \qqand p\coalg\times q\coalg\to(p\otimes q)\coalg. \] Thus the structure maps $\yon\to[p,p]$ and $[p,q]\otimes[q,r]\to[p,r]$ induce the identity an
- **`\smset`** (= `\Cat{Set}`) (introduced in \cref{sec.poly}) -- \smset\to\smset$ of the form $\sum_{i: I}\yon^{A_i}$, where $I$ is the set of \emph{positions} and $A_i$ is the set of \emph{directions} at position $i$. Since $p(1)=\sum_{i:I}1=I$, we use the compact
- **`\vect`** (= `\Cat{Vect}`) (introduced in \cref{sec.manifolds_notation}) -- \vect\to\mfd$, treating finite-dimensional real vector spaces as manifolds. The direct sum $(0,\oplus)$ structure in $\vect$ is (a coproduct and) a product, and the inclusion is product preserving. By

## Functors

- **`\List`** (= `\Fun{List}`) (introduced in \cref{sec.wd_operads}) -- \List(O)\times O$, because every ``box'' has a list of inputs and an output. } A $\cat{W}$-algebra $\cat{C}\colon\cat{W}\to\smset$ assigns to each box $b$ the set $\cat{C}(b)$ of ``fillers'' and to ea

## Structures

- **`\nn`** (= `\mathbb{N}`) (introduced in \cref{sec.prelim}) -- \nn,\rr$, as does the only bicategory we work with, $\org$. In a symmetric monoidal category $(\cat{C},I,\otimes)$, a \emph{$(\otimes)$-comonoid} is an object $c$ equipped with a coassociative comulti
- **`\rr`** (= `\mathbb{R}`) (introduced in \cref{sec.prelim}) -- \rr$, as does the only bicategory we work with, $\org$. In a symmetric monoidal category $(\cat{C},I,\otimes)$, a \emph{$(\otimes)$-comonoid} is an object $c$ equipped with a coassociative comultiplic
- **`\yon`** (= `\mathcal{y}`) (introduced in \cref{sec.TT}) -- \yon^V\cong\cot{V}$ in \cref{prop.pnla_polynomial}. \begin{remark}[Generalization to nilpotent Lie algebras]\label{rmk.pnla_generalization} \Cref{sec.pnla,sec.TT} generalize from paired vector spaces 
- **$(\otimes)$-comonoid** (introduced in \cref{sec.prelim}) -- \emph{$(\otimes)$-comonoid} is an object $c$ equipped with a coassociative comultiplication $\delta_c\colon c\to c\otimes c$ and a counit $\varepsilon_c\colon c\to I$ satisfying the counit law; a \emp
- **$(\otimes)$-monoid** (introduced in \cref{sec.prelim}) -- \emph{$(\otimes)$-monoid} is the dual. A \emph{comonoid homomorphism} $f\colon c\to c'$ satisfies $f\then\varepsilon_{c'}=\varepsilon_c$ and $f\then\delta_{c'}=\delta_c\then(f\otimes f)$. A \emph{supp
- **$\Fun{T** (introduced in \cref{def.T_monoid}) -- \emph{$\Fun{T}$-monoid in $\cat{C}$} is a $\otimes$-monoid $(z,e_z,m_z)$ in $\cat{C}$ \[ \begin{tikzcd}[ampersand replacement=\&] z\ar[r,"\eta_z"]\ar[dr,equal]\& \Fun{T}z\ar[d,"\alpha"]\\ \& z \end{ti
- **$\tri$-comonoid** (introduced in \cref{sec.comonads}) -- \emph{$\tri$-comonoid} in $\poly$ is a comonoid for the substitution product: a polynomial $c$ equipped with a counit $\epsilon\colon c\to\yon$ and comultiplication $\delta\colon c\to c\tri c$ satisfy
- **$p$-coalgebra** (introduced in \cref{sec.coalgebras}) -- \emph{$p$-coalgebra} is a pair $(S,\beta)$ where $S:\smset$ is a set and $\beta\colon S\to p(S)$ is a function. We call elements of $S$ states; given a state $s:S$, the map assigns a \emph{readout} po
- **[Introduction to be written.]** (introduced in \cref{ch.intro}) -- \emph{[Introduction to be written.]} \section*{Acknowledgments} Thanks to Lukas Barth, Scott Viteri, and Jake Arujo-Simon. I also thank Claude and Codex for assistance in editing the document, and var
- **action** (introduced in \cref{sec.org}) -- \emph{action} function \[\act^\beta\colon S\to\poly(p,q)=\ihom{p,q}(1)\] and, for each state $s:S$, an \emph{update} function \[\upd^\beta_s\colon\sum_{I:p(1)}q\left[\act^\beta_s(I)\right]\to S.\] For
- **canonical symplectic pairing** (introduced in \cref{prop.canonical_symplectic_pairing}) -- \emph{canonical symplectic pairing} \begin{equation}\label{eqn.canonical_form} \pairing{(v,\xi),(v',\xi')}_{V\oplus V^*} \;=\; \xi'(v)-\xi(v'). \end{equation} \end{proposition} \begin{proof} The form 
- **codiscrete category** (introduced in \cref{ex.store}) -- \emph{codiscrete category}) on $S$. \end{example} \subsection{Polynomial coalgebras as dynamical systems}\label{sec.coalgebras} For a polynomial $p$, a \emph{$p$-coalgebra} is a pair $(S,\beta)$ where
- **comonoid homomorphism** (introduced in \cref{sec.prelim}) -- \emph{comonoid homomorphism} $f\colon c\to c'$ satisfies $f\then\varepsilon_{c'}=\varepsilon_c$ and $f\then\delta_{c'}=\delta_c\then(f\otimes f)$. A \emph{supply of comonoids}~\cite{fong2019supplying}
- **composition** (introduced in \cref{sec.comonads}) -- \emph{composition}) \emph{product} on $\poly$ is defined by $(p\tri q)(S)\coloneqq p(q(S))$, with unit $\yon$. Using the expanded form of \eqref{eqn.poly}: \begin{equation}\label{eqn.substitution} p\t
- **constant** (introduced in \cref{sec.poly}) -- \emph{constant}. One can derive from the universal property of coproducts and the Yoneda lemma that natural transformations $\varphi\colon p\to q$ are given by: \begin{equation}\label{eqn.poly_map} \p
- **cotangent functor** (introduced in \cref{def.cot}) -- \emph{cotangent functor} $\cot\colon\mfd\to\poly$ sends a smooth manifold $M$ to \begin{equation}\label{eqn.cot_def} \cot{M}\coloneqq\sum_{m\in M}\yon^{T^*_mM} \end{equation} where $T^*_mM$ is the cot
- **cotangent internalization** (introduced in \cref{subsec.dynamics_functor}) -- \emph{cotangent internalization} functor: it first applies $\cot$ to a potentialized manifold lens, then uses $\Theta_{\potd}$ to internalize the resulting potentialized polynomial lens as a parameter
- **counital projection** (introduced in \cref{subsec.backwards}) -- \emph{counital projection} $\sigma_{X,Y}\then\Fun{T}(\varepsilon_X\otimes\id_Y)=\varepsilon_X\otimes\id_{\Fun{T}Y}$: \begin{equation}\label{eqn.counital_projection} \begin{tikzcd} X\otimes\Fun{T}Y\ar[
- **covector fields** (introduced in \cref{subsec.dynamics_functor}) -- \emph{covector fields} on $M$. A smooth map $f\colon M\to N$ induces a \emph{pullback} $f^*\colon\Omega(N)\to\Omega(M)$ defined by $(f^*\omega)(m)\coloneqq(T_m f)^\top\omega(f(m))$. \paragraph{The act
- **differential** (introduced in \cref{sec.manifolds_notation}) -- \emph{differential} of $U$: \begin{equation}\label{eqn.differential} dU\colon M\to T^*M,\qquad dU|_m\coloneqq(T_m U)^\top(+1)\in T^*_mM. \end{equation} That is, $dU|_m$ is the covector at $m$ whose pa
- **differential at $m$** (introduced in \cref{sec.manifolds_notation}) -- \emph{differential at $m$}---the restriction of $Tf$ to the fiber over $m\in M$--- and its transpose.% \footnote{What we call $T_m f$ is often denoted $Df_m$.} When the codomain is $\rr$, the tangent 
- **directions** (introduced in \cref{sec.poly}) -- \emph{directions} at position $i$. Since $p(1)=\sum_{i:I}1=I$, we use the compact notation $p(1)$ for the position set and $p[i]\coloneqq A_i$ for the direction set at $i: p(1)$, so that \begin{equati
- **Dirichlet product** (introduced in \cref{sec.poly}) -- \emph{Dirichlet product} is \begin{equation}\label{eqn.dirichlet} p\otimes q\coloneqq\sum_{(i,j):p(1)\times q(1)}\yon^{p[i]\times q[j]}\;. \end{equation} The unit is $\yon=\yon^1$. The Dirichlet produ
- **dynamic** (introduced in \cref{sec.wd_operads}) -- \emph{dynamic} versions of the structures in \eqref{eqn.wd_examples}, in which the ``fillers'' of a box are ``morphisms that change in time'', i.e.\ coalgebras. In \cite{shapiro2022dynamic}, a dynamic
- **dynamical realization** (introduced in \cref{lem.poly_to_org}) -- \emph{dynamical realization} functor. \end{lemma} \begin{proof} Let $F\colon\pvect\to\smset_\cong$ be the strong monoidal composite $\pvect\To{T^*}\pvect\to\smset_\cong$, where the second functor forg
- **exponential** (introduced in \cref{sec.TT}) -- \emph{exponential} of a paired vector space $(V,\sharp_V)$ is the time-1 flow of the constant left-invariant vector field $\sharp_V(\xi)$ from base point $v$: \begin{equation}\label{eqn.flow_cot} \exp
- **flat** (introduced in \cref{def.pnla}) -- \emph{flat} map. The corresponding nondegenerate bilinear form on $V$, the \emph{pairing}, is \begin{equation}\label{eqn.pairing} \pairing{v,v'}_V\coloneqq\flat_V(v)(v')\colon V\otimes V\to\rr \end{eq
- **global differential** (introduced in \cref{sec.manifolds_notation}) -- \emph{global differential}), and \[ T_m f\colon T_mM\to T_{f(m)}N \qqand (T_m f)^\top\colon T^*_{f(m)}N\to T^*_mM \] for the \emph{differential at $m$}---the restriction of $Tf$ to the fiber over $m\i
- **Hamilton's equations** (introduced in \cref{prop.hamilton}) -- \emph{Hamilton's equations} \[ \dot v=\partial_\xi H,\qquad \dot\xi=-\partial_v H, \] and $H$ is conserved along them: $dH/dt=0$. \end{proposition} \begin{proof} By \cref{def.hamiltonian_vector_field}
- **Hamiltonian** (introduced in \cref{def.hamiltonian_vector_field}) -- \emph{Hamiltonian}). The \emph{Hamiltonian vector field of $H$} is the composite $X_H$ obtained as follows: \[ \begin{tikzcd}[column sep=1.8em, row sep=2.4em, ampersand replacement=\&] T^*V \ar[d, das
- **Hamiltonian vector field of $H$** (introduced in \cref{def.hamiltonian_vector_field}) -- \emph{Hamiltonian vector field of $H$} is the composite $X_H$ obtained as follows: \[ \begin{tikzcd}[column sep=1.8em, row sep=2.4em, ampersand replacement=\&] T^*V \ar[d, dashed, "{X_H}"'] \ar[r, "{(
- **harmonic** (introduced in \cref{sec.spring_first_pass}) -- \emph{harmonic} potential. \paragraph{The wiring diagram.} For the wiring diagram, we start in $\Lens{\finset\op}$; see \cref{ex.lens_finsetop}. The $K$-ary series composition shown in \eqref{eqn.wave
- **harmonic spring** (introduced in \cref{sec.spring_first_pass}) -- \emph{harmonic spring} covector field on the lefthand side, \[\omega_\boxob(v_0)=d\bigl[\tfrac{\kappa}{2}(v_1-v_0)^2\bigr]\big|_{v_0}=-\kappa(v_1-v_0),\] comes from \eqref{eqn.omegaprime} (with $\inpt
- **heavy-ball method** (introduced in \cref{sec.heavy_ball}) -- \emph{heavy-ball method} of Polyak~\cite{polyak1964momentum}. Take $V\coloneqq\rr^k$, sharp $\sharp_V(p)\coloneqq p/m$ for an inertial mass $m>0$, vacuous ports $\inpt M=\outp M=\rr^0$ with vacuous $\
- **homomorphic** (introduced in \cref{sec.prelim}) -- \emph{homomorphic} if every morphism of $\cat C$ is a comonoid homomorphism for the supplied structures. \begin{lemma}\label{lem.cartesian_homomorphic_supply} Any cartesian monoidal category canonical
- **hyperparameter** (introduced in \cref{ex.mms_finsetop}) -- \emph{hyperparameter} that decides a sort of internal wiring. Indeed, the data of a morphism in the coKleisli category $\Lens{\finset\op}^{S\times-}$ is similar to that in \cref{eqn.lens_finsetop}, ex
- **internal hom** (introduced in \cref{sec.poly}) -- \emph{internal hom} is \begin{equation}\label{eqn.dirichlet_hom} \ihom{p,q}=\prod_{i: p(1)}\;\sum_{j: q(1)}\;\prod_{e: q[j]}\;\sum_{d: p[i]}\yon \end{equation} satisfying $\poly(r\otimes p,\, q)\cong\
- **Legendre projection** (introduced in \cref{def.legendre_projection}) -- \emph{Legendre projection} $\rho_V\colon\cot{T^*V}\to\cot{V}$ has position-action $(v,\xi)\mapsto v$ and direction-action at $(v,\xi)$ \[ T^*_vV\cong V^*\To{\xi_V\mapsto(\xi_V,\,\sharp_V(\xi))}V^*\opl
- **Legendre refinement** (introduced in \cref{lem.para_rho}) -- \emph{Legendre refinement} functor. \end{lemma} \begin{proof} Apply \cref{prop.para_strong_induced,prop.para_square} to $\rho$. \end{proof} \begin{lemma}\label{lem.poly_to_org} With the action $V\cdot
- **Legendre transform** (introduced in \cref{rmk.legendre}) -- \emph{Legendre transform} of classical mechanics: $\flat_V$ sends a velocity to its conjugate momentum, and $\sharp_V$ sends a momentum to the corresponding velocity.\qedhere \end{remark} The \emph{ex
- **momentum** (introduced in \cref{sec.cot}) -- \emph{momentum} $\xi\in V^*$. The cotangent space decomposes as \[ T^*_{(v,\xi)}(T^*V)\cong T^*_vV\oplus T^*_\xi(V^*)\cong V^*\oplus V, \] again by \eqref{eqn.tangent_vec}, using $(V^*)^*\cong V$ on t
- **monomial** (introduced in \cref{sec.poly}) -- \emph{monomial} is a polynomial of the form $M\yon^N=\sum_{m\in M}\yon^N$; we refer to $\yon^N$ as a \emph{representable} and to $M=M\yon^0$ as a \emph{constant}. One can derive from the universal pro
- **pair groupoid** (introduced in \cref{ex.store}) -- \emph{pair groupoid} (or \emph{codiscrete category}) on $S$. \end{example} \subsection{Polynomial coalgebras as dynamical systems}\label{sec.coalgebras} For a polynomial $p$, a \emph{$p$-coalgebra} is
- **paired nilpotent Lie algebras** (introduced in \cref{rmk.pnla_generalization}) -- \emph{paired nilpotent Lie algebras} (PN Lie Algebras): pairs $(\lie g,\pairing{-,-})$ of a finite-dimensional nilpotent real Lie algebra and a pairing on its underlying vector space. Replacing $\pvec
- **paired vector space** (introduced in \cref{def.pnla}) -- \emph{paired vector space} is a finite-dimensional real vector space $V$ together with a linear isomorphism $\sharp_V\colon V^*\To{\cong}V$, the \emph{sharp} map, with inverse $\flat_V\coloneqq\sharp_
- **pairing** (introduced in \cref{def.pnla}) -- \emph{pairing}, is \begin{equation}\label{eqn.pairing} \pairing{v,v'}_V\coloneqq\flat_V(v)(v')\colon V\otimes V\to\rr \end{equation} The three forms $\sharp_V$, $\flat_V$, and $\pairing{-,-}_V$ packag
- **pairing-preservation equation** (introduced in \cref{def.pnla}) -- \emph{pairing-preservation equation} holds: \begin{equation}\label{eqn.pairing_triangle} \phi\circ\sharp_V\circ\phi^*=\sharp_W\colon W^*\to W \qedhere \end{equation} \end{definition} \begin{propositio
- **parameterized maps** (introduced in \cref{sec.para_general}) -- \emph{parameterized maps} in $\cat D$. The identity on $x$ has parameter $I$ and the action unitor $I\cdot x\cong x$. The composite of $(a,f)\colon x\to y$ and $(b,g)\colon y\to z$ has parameter $b\ot
- **polynomial comonad** (introduced in \cref{sec.comonads}) -- \emph{polynomial comonad}. Polynomial comonads are---up to isomorphism---small categories. Writing $c=\sum_{i:c(1)}\yon^{c[i]}$, the positions $c(1)$ are the objects of some category $\cat{C}$, and fo
- **polynomial functor** (introduced in \cref{sec.poly}) -- \emph{polynomial functor} is an endofunctor $p\colon\smset\to\smset$ of the form $\sum_{i: I}\yon^{A_i}$, where $I$ is the set of \emph{positions} and $A_i$ is the set of \emph{directions} at position
- **position** (introduced in \cref{sec.cot}) -- \emph{position} $v\in V$ and \emph{momentum} $\xi\in V^*$. The cotangent space decomposes as \[ T^*_{(v,\xi)}(T^*V)\cong T^*_vV\oplus T^*_\xi(V^*)\cong V^*\oplus V, \] again by \eqref{eqn.tangent_vec}
- **positions** (introduced in \cref{sec.poly}) -- \emph{positions} and $A_i$ is the set of \emph{directions} at position $i$. Since $p(1)=\sum_{i:I}1=I$, we use the compact notation $p(1)$ for the position set and $p[i]\coloneqq A_i$ for the directio
- **potentialized lenses** (introduced in \cref{def.potlens}) -- \emph{potentialized lenses} is denoted and defined by \[ \potlens\coloneqq\para{\pvect}{\lmfd^\rr}. \qedhere \] \end{definition} \begin{lemma}\label{lem.potlens_to_para_poly} There is a lax symmetric 
- **potentialized manifold lenses** (introduced in \cref{subsec.potentialized_lenses}) -- \emph{potentialized manifold lenses}. A map $\lensob p\to\lensob q$ in $\Lens{\poly}^\rr$ is a pair \[ \outp f\colon\outp p\to\outp q,\qquad \inpt f\colon\outp p\otimes\inpt q\to\cot{\rr}\otimes\inpt 
- **potentialized polynomial lenses** (introduced in \cref{subsec.potentialized_lenses}) -- \emph{potentialized polynomial lenses}. \begin{lemma}\label{lem.cot_lifts_lens_potential} The strong symmetric monoidal functor $\cot\colon\mfd\to\poly$ lifts to a strong symmetric monoidal functor \[
- **potentials** (introduced in \cref{subsec.potentialized_lenses}) -- \emph{potentials}. By \cref{lem.alpha_constant}, once we set $z\coloneqq\yon$, the remaining choice of $(\cot{\rr}\otimes\blank)$-monoid structure is a real number $r:\rr$, equivalently a constant cov
- **product** (introduced in \cref{sec.comonads}) -- \emph{product} on $\poly$ is defined by $(p\tri q)(S)\coloneqq p(q(S))$, with unit $\yon$. Using the expanded form of \eqref{eqn.poly}: \begin{equation}\label{eqn.substitution} p\tri q=\sum_{i: p(1)}\
- **pullback** (introduced in \cref{subsec.dynamics_functor}) -- \emph{pullback} $f^*\colon\Omega(N)\to\Omega(M)$ defined by $(f^*\omega)(m)\coloneqq(T_m f)^\top\omega(f(m))$. \paragraph{The action of $\Phi$ on objects.} The functor $\Phi$ acts on objects as follow
- **readout** (introduced in \cref{sec.coalgebras}) -- \emph{readout} position $r_s\coloneqq\beta_1(s):p(1)$ and an \emph{update} function $\bk{\beta}_s\colon p[r_s]\to S$ sending each direction at $r_s$ to a new state. For example when $p=A\yon^B$ then a
- **representable** (introduced in \cref{sec.poly}) -- \emph{representable} and to $M=M\yon^0$ as a \emph{constant}. One can derive from the universal property of coproducts and the Yoneda lemma that natural transformations $\varphi\colon p\to q$ are give
- **semi-category** (introduced in \cref{sec.plenspot_ops}) -- \emph{semi-category} we mean almost a category, except no identities (like a semigroup is a group without identities). So our goal is to show that $\Lens{C}$ interprets diagrams that look like this: \
- **sharp** (introduced in \cref{def.pnla}) -- \emph{sharp} map, with inverse $\flat_V\coloneqq\sharp_V\inv\colon V\to V^*$, the \emph{flat} map. The corresponding nondegenerate bilinear form on $V$, the \emph{pairing}, is \begin{equation}\label{e
- **state set** (introduced in \cref{def.pq_coalg}) -- \emph{state set}. \end{definition} Unwinding this using \eqref{eqn.dirichlet}, $\beta=(\act^\beta,\upd^\beta)$ decomposes as an \emph{action} function \[\act^\beta\colon S\to\poly(p,q)=\ihom{p,q}(1)\]
- **static** (introduced in \cref{sec.spring_second_pass}) -- \emph{static}: a function $1\to[\Phi(\boxob)^{\otimes K},\Phi(\boxob)](1)$ is equivalently (see \eqref{eqn.dirichlet_hom}) a polynomial map $\Phi(\boxob)^{\otimes K}\to\Phi(\boxob)$, i.e.\ \[\sum_{(\v
- **store action** (introduced in \cref{def.store_action}) -- \emph{store action} by \[ (\cdot)\colon\smset_\cong\times\poly\to\poly \where S\cdot p\coloneqq S\yon^S\otimes p. \qedhere \] \end{definition} \subsection{Cotangent bundles as polynomials}\label{sec.c
- **strong** (introduced in \cref{subsec.backwards}) -- \emph{strong} if there are natural maps $\sigma\colon X\otimes\Fun{T}Y\to\Fun{T}(X\otimes Y)$, satisfying four diagrams~\cite{kock1972strong}. If moreover $X$ carries a counit $\varepsilon_X\colon X\t
- **substitution** (introduced in \cref{sec.comonads}) -- \emph{substitution} (or \emph{composition}) \emph{product} on $\poly$ is defined by $(p\tri q)(S)\coloneqq p(q(S))$, with unit $\yon$. Using the expanded form of \eqref{eqn.poly}: \begin{equation}\lab
- **supply of comonoids** (introduced in \cref{sec.prelim}) -- \emph{supply of comonoids}~\cite{fong2019supplying} is a choice, compatible with $\otimes$, of a cocommutative such structure on every object of $\cat{C}$. A supply is \emph{homomorphic} if every morp
- **tangent map** (introduced in \cref{sec.manifolds_notation}) -- \emph{tangent map} (also called the \emph{global differential}), and \[ T_m f\colon T_mM\to T_{f(m)}N \qqand (T_m f)^\top\colon T^*_{f(m)}N\to T^*_mM \] for the \emph{differential at $m$}---the restri
- **the differential of $f$ at $v$** (introduced in \cref{sec.manifolds_notation}) -- \emph{the differential of $f$ at $v$}. We also use the canonical finite-dimensional dual-sum identification \begin{equation}\label{eqn.canonical_dual_sum} (V\oplus V^*)^*\cong V^*\oplus V,\qquad \lamb
- **underlying operad** (introduced in \cref{sec.wd_operads}) -- \emph{underlying operad} of a symmetric monoidal category $(\cat{C},I,\otimes)$ has the same objects as $\cat{C}$ and multimorphisms $\cat{C}(a_1,\ldots,a_n;b)\coloneqq\cat{C}(a_1\otimes\cdots\otimes 
- **update** (introduced in \cref{sec.coalgebras}) -- \emph{update} function $\bk{\beta}_s\colon p[r_s]\to S$ sending each direction at $r_s$ to a new state. For example when $p=A\yon^B$ then a $p$-coalgebra $S\to AS^B$ is equivalently a readout function
- **value object** (introduced in \cref{def.T_monoid}) -- \emph{value object}. \end{definition} \begin{remark}\label{rmk.T_monoid_EM} A $\Fun{T}$-monoid in $\cat{C}$ is the same data as a monoid in the Eilenberg--Moore category $\cat{C}^{\Fun{T}}$, equivalen
