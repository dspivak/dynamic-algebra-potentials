# Plan: omnibus "Further reach" environment (replaces rmk.further_devices)

## State
- `CDLM.tex` `rmk.further_devices` (~lines 2670–2678, after `rmk.tank_electrical`, before `ex.lotka_volterra`) currently holds a 3-device draft (DC motor / transistor / amplifier). **Replace it entirely** (`%`-marked edit protocol; report line blocks only; commit after approval).
- Conclusion's DC-motor paragraph already commented out (~line 4303). Leave.
- All nine claims below were implemented and machine-verified in dap (public repo github.com/dspivak/dap, branch `faithful-machine`, through commit `ba5f571`; full suite green). New modules: kuramoto, replicator, moleculardyn, control, lc_circuit, hopfield, reaction_network, dc_motor, amplifier (+ logic from earlier). Each has a one-step-exactness test pinning conventions.

## Decisions (David-approved)
1. **One omnibus environment, in place.** David leans it may be an `example` rather than `remark` (content = constructions, matching the paper's example idiom; assistant agrees: example). Title like "Further reach". Keep label or relabel `ex.further_reach` — update any refs.
2. **Epistemic frame** (opening sentences): the constructions were built and machine-checked (tests in the dap repo, same footnote/hedge idiom as ch.applications' footnote); the author has not verified them by hand but believes them correct — diligence done "using helpers some people don't trust," phrased tactfully, not in those words. Not "use at your own risk."
3. **Domain grouping, mechanisms up front.** Frame paragraph names the mechanisms that do the work: opposite-signed sharps (DC motor, cf. Lotka–Volterra), degenerate non-symmetric sharp = unilaterality (transistor; amplifier dynamics), state-dependent sharps (replicator, Hopfield, CRN), phase reading (magnetism/LC, MD).
4. **Format**: `\begin{description}`-style, one `\item[Device.]` each, STRICTLY PARALLEL fields: background · interfaces (boxes + lens types) · reactive space Q + sharp · outp f / inpt f · potential · one punchline. Compress for simple ones but keep parallel shape.
5. **Only true content** — final encodings only; no refuted variants, no obstructions, no discovery path (per David's no-obstruction-content rule).
6. **Keep all items** (incl. impedance control). Order: transistors & logic gates, feedback amplifier, LC/magnetism, DC motor, molecular dynamics, impedance control, Kuramoto & power grids, reaction networks, replicator, Hopfield.
7. **Closer**: sentence pointing to `ch.applications` — a few examples are worked out in detail there, including the dynamics.
8. Length: ~page-plus is accepted. **Each item is ~three sentences** — no physics background assumed, no named constants or explicit potential formulas; the "verified facts" below are for correctness, not for inclusion.

## Verified facts per item (use these; corrected where audits refuted drafts)
- **Transistors/NAND** (cut examples recoverable at commit `3bdf683`, ex.mosfet/ex.nand): state (ℝ², ♯(ξ,ξ')=(ξ',0)) constant, neither symmetric nor invertible (def.rvect asks neither); drain voltage enters potential and returned covector without entering reaction's image; four transistors + two capacitors + rail ⇒ NAND. Verified in dap/logic.py (also a latch).
- **Black amplifier** (CORRECTED): gain v* = A/(1+Aβ)·e_src and desensitization (dG/G)/(dA/A)=1/(1+Aβ) are EQUILIBRIUM facts of the composite (fixed point of Φconf) — they do NOT need unilaterality (bilateral control matches). Unilaterality's verified role is DYNAMICAL: stability threshold dt·κ(1+Aβ)=2 vs bilateral (1+Aβ)², and output-load rejection. THD suppression measured 90.7 vs folklore 1+Aβ=101, bracketed by [1+Aβg'_min, 1+Aβ]. Summing junction = linear static box (not finset wiring). No power supply — gain is relaxation coefficient, not power gain.
- **LC/magnetism**: cell: state q (charge), sharp ξ/L, U=q²/2C, out_f=q. Under Φphase momentum = flux linkage (ξ_n = L(q_{n+1}−q_n) exact), magnetic energy = kinetic term, period 2π√(LC) (−ω²/24 Euler bias resolved); under Φconf same arrangement = RC relaxation (integrator assigns the sharp's role: mass vs mobility = wave/heat dichotomy). Coupled pair: both normal modes exact.
- **DC motor** (CORRECTED — old T*ℝ/−♯S sentence is FALSE, do not use): two 1-dim boxes: electrical state φ, sharp +dt·L, U=(R/2)i²−Vi, reports i=φ/L; mechanical state p, sharp −dt·J, U=−((b/2)ω²+τω), reports ω=p/J; stateless gyrator box U_c=K·i·ω. Opposite-signed sharps are forced (potential couplings have symmetric cross-Hessian); Euler-exact vs van der Schaft Ex. 2.5 (already in bib: vanderschaft2014port). Stall = degenerate sharp 0. Both boxes report their momentum.
- **Molecular dynamics**: particle boxes (state ℝ^d, sharp (dt²/m)I), stateless LJ pair boxes U=4ε((σ/r)¹²−(σ/r)⁶) reading two reported positions, graph wiring; Φleap = velocity-Verlet exactly; equilibrium 2^{1/6}σ; energy band 34.6× tighter than Φphase.
- **Impedance control**: stateless controller box, potential (k/2)(q_reported − s)², setpoint s = run-time outer input; Φconf settles to s (geometric), Φphase oscillates about s; reaction covector on setpoint port = −k(q−s) (Newton's 3rd law at boundary). Stateless ⇒ P-law only.
- **Kuramoto/grids**: oscillator boxes (state θᵢ, sharp dt·c, own potential −ωᵢθᵢ, broadcast out_f), stateless edge boxes U=−K cos(θᵢ−θⱼ); Φconf = Kuramoto (locking at K>|Δω|/2, δ*=arcsin(Δω/2K)); Φphase same arrangement = swing equation (grids).
- **Reaction networks** (state frontier plainly, no no-go dwelling): every reversible detailed-balanced mass-action network is Φconf of one box: sharp = Mielke–Onsager K(x)=Σ_r Λ(f_r,b_r)ζ_rζ_rᵀ (log-mean; symmetric PSD state-dependent), potential = free energy Σxᵢ(log(xᵢ/xᵢ^eq)−1); one step = exact mass-action Euler step. Compositional species-box/reaction-box form covers exactly the unimolecular reversible case (species box: sharp dt·xᵢ^eq, reports activity xᵢ/xᵢ^eq; reaction box (w_r/2)(aᵢ−aⱼ)²).
- **Replicator**: state x∈ℝⁿ, Shahshahani sharp ξ↦dt(diag(x)−xxᵀ)ξ (state-dependent, symmetric, degenerate), U=−½xᵀAx, A symmetric (potential games); step = exact Euler replicator; converges to ESS (Hawk–Dove via symmetric representative).
- **Hopfield**: neuron boxes (state uᵢ, state-dependent sharp dt/g'(uᵢ)=dt·cosh²uᵢ, out_f=tanh uᵢ, leak potential u·g(u)−log cosh u−Iᵢg(u)), stateless synapse boxes −W_ij VᵢVⱼ; emergent composite potential = Hopfield's 1984 energy EXACTLY; flow = natural-gradient flow in metric g'(u)du²; retrieval verified (gain-2 Hebbian).
- Cross-cutting honest clause (say once): the framework steps in unit time, so dt rides inside the sharp.

## Also pending / parked
- Three-column table idea for rmk.tank_electrical (hydraulics | electricity | mechanics; magnetism = the Φphase reading of the whole table, in words below it) — David: "maybe not worth it"; unresolved.
- Style: NOTATION.md canonical; univocity; types-not-values; short statements; \eqref labeled displays; verify cites (vanderschaft2014port exists; NO Black citation in bib yet — don't add uncited claims to bib without checking).
- After draft approved: one commit per chunk. dap code: consider PR/merge of `faithful-machine` to main (David's call).
